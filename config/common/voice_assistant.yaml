substitutions:
  # Phases of the Voice Assistant
  voice_assist_idle_phase_id: '1'
  voice_assist_waiting_for_command_phase_id: '2'
  voice_assist_listening_for_command_phase_id: '3'
  voice_assist_thinking_phase_id: '4'
  voice_assist_replying_phase_id: '5'
  voice_assist_not_ready_phase_id: '10'
  voice_assist_error_phase_id: '11'

globals:
  - id: voice_assistant_phase
    type: int
    restore_value: no

microphone:
  - platform: satellite1
    id: sat1_mics
    sample_rate: 48000
    i2s_din_pin: GPIO15
    bits_per_sample: 32bit
    channel: stereo
    pdm: false
    adc_type: external
    i2s_audio_id: i2s_shared

micro_wake_word:
  id: mww
  models:
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/hey_jarvis.json
      id: hey_jarvis
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/okay_nabu.json
      id: okay_nabu
    - model: https://fph-firmware-assets.s3.us-east-1.amazonaws.com/wake-word/stop.json
      id: stop
      internal: true
  vad:
  microphone:
    microphone: sat1_mics
    channels: 1
    gain_factor: 6
  stop_after_detection: false
  on_wake_word_detected:
    - if:
        condition:
          switch.is_off: master_mute_switch
        then:
          - if:
              condition:
                switch.is_on: timer_ringing
              then:
                - switch.turn_off: timer_ringing
              else:
                # The "stop" model is a cancel signal, not a session-start.
                # It acts ONLY when there is something to stop: while a reply
                # is audibly playing, or in the follow-up window.
                - if:
                    condition:
                      lambda: 'return wake_word == "Stop" || wake_word == "stop";'
                    then:
                      - if:
                          condition:
                            lambda: 'return id(voice_assistant_phase) != ${voice_assist_idle_phase_id} && id(va)->turn_has_reply_audio();'
                          then:
                            - lambda: 'id(va)->send_interrupt();'
                            - speaker.stop:
                                id: media_resampling_speaker
                            - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
                            - script.execute: control_leds
                            - micro_wake_word.disable_model: stop
                    else:
                      # Non-stop wake word: if already replying, barge-in
                      - if:
                          condition:
                            lambda: 'return id(voice_assistant_phase) == ${voice_assist_replying_phase_id};'
                          then:
                            - lambda: 'id(va)->send_interrupt();'
                            - speaker.stop:
                                id: media_resampling_speaker
                            - if:
                                condition:
                                  switch.is_on: wake_sound
                                then:
                                  - script.execute:
                                      id: play_sound
                                      priority: true
                                      sound_file: "wake_word_triggered_sound"
                                  - wait_until:
                                      condition:
                                        not:
                                          media_player.is_announcing:
                                            id: external_media_player
                                      timeout: 2s
                                  - delay: !lambda "return id(va)->get_wake_open_delay_ms();"
                            - lambda: 'id(va)->start_session();'
                            - lambda: id(voice_assistant_phase) = ${voice_assist_listening_for_command_phase_id};
                            - script.execute: control_leds
                          else:
                            # Backend down? Don't pretend to listen.
                            - if:
                                condition:
                                  lambda: 'return !id(va)->is_connected();'
                                then:
                                  - script.execute:
                                      id: play_sound
                                      priority: true
                                      sound_file: "wake_word_triggered_sound"
                                else:
                                  # Regular wake word: play chime, start session
                                  - speaker.stop:
                                      id: media_resampling_speaker
                                  - if:
                                      condition:
                                        switch.is_on: wake_sound
                                      then:
                                        - script.execute:
                                            id: play_sound
                                            priority: true
                                            sound_file: "wake_word_triggered_sound"
                                        - wait_until:
                                            condition:
                                              not:
                                                media_player.is_announcing:
                                                  id: external_media_player
                                            timeout: 2s
                                        - delay: !lambda "return id(va)->get_wake_open_delay_ms();"
                                  - lambda: 'id(va)->start_session();'
                                  - lambda: id(voice_assistant_phase) = ${voice_assist_waiting_for_command_phase_id};
                                  - script.execute: control_leds

va_client:
  id: va
  url: ${va_url}
  microphone: sat1_mics
  mic_channel: 0
  barge_in: false
  speaker: media_resampling_speaker
  on_phase:
    - lambda: |-
        if (phase == "idle") {
          id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
        } else if (phase == "listening") {
          id(voice_assistant_phase) = ${voice_assist_listening_for_command_phase_id};
        } else if (phase == "thinking") {
          id(voice_assistant_phase) = ${voice_assist_thinking_phase_id};
        } else if (phase == "replying") {
          id(voice_assistant_phase) = ${voice_assist_replying_phase_id};
        }
    - script.execute: control_leds
    # Re-arm wake word on return to idle
    - if:
        condition:
          lambda: 'return phase == "idle";'
        then:
          - micro_wake_word.start:
    # Arm "stop" during reply
    - if:
        condition:
          lambda: 'return phase == "replying";'
        then:
          - micro_wake_word.enable_model: stop
          - script.stop: followup_window_watchdog
    # Cancel watchdog on new listening
    - if:
        condition:
          lambda: 'return phase == "listening";'
        then:
          - script.stop: followup_window_watchdog
    # Disarm stop and start watchdog on idle
    - if:
        condition:
          lambda: 'return phase == "idle";'
        then:
          - micro_wake_word.enable_model: stop
          - script.execute: followup_window_watchdog
  on_repeated_failure:
    - script.execute:
        id: play_sound
        priority: true
        sound_file: "wake_word_triggered_sound"
  on_followup_opened:
    - if:
        condition:
          switch.is_on: wake_sound
        then:
          - script.execute:
              id: play_sound
              priority: true
              sound_file: "wake_word_triggered_sound"
          - wait_until:
              condition:
                not:
                  media_player.is_announcing:
                    id: external_media_player
              timeout: 2s
          - delay: 800ms
    - lambda: 'id(va)->commit_followup_mic();'
    - lambda: id(voice_assistant_phase) = ${voice_assist_listening_for_command_phase_id};
    - script.execute: control_leds
    - micro_wake_word.enable_model: stop
    - script.execute: followup_window_watchdog

script:
  # Watchdog to disarm stop word after follow-up window expires
  - id: followup_window_watchdog
    mode: restart
    then:
      - delay: 12000ms
      - if:
          condition:
            lambda: |-
              return id(voice_assistant_phase) == ${voice_assist_idle_phase_id} ||
                     id(voice_assistant_phase) == ${voice_assist_listening_for_command_phase_id};
          then:
            - micro_wake_word.disable_model: stop
            - lambda: id(voice_assistant_phase) = ${voice_assist_idle_phase_id};
            - script.execute: control_leds

select:
  - platform: template
    id: mww_sensitivity_select
    name: "Wake word sensitivity"
    optimistic: true
    initial_option: Slightly sensitive
    restore_value: true
    entity_category: config
    options:
      - Slightly sensitive
      - Moderately sensitive
      - Very sensitive
    on_value:
      lambda: |-
        if (x == "Slightly sensitive") {
          id(okay_nabu).set_probability_cutoff(217);
          id(hey_jarvis).set_probability_cutoff(247);
        } else if (x == "Moderately sensitive") {
          id(okay_nabu).set_probability_cutoff(176);
          id(hey_jarvis).set_probability_cutoff(235);
        } else if (x == "Very sensitive") {
          id(okay_nabu).set_probability_cutoff(143);
          id(hey_jarvis).set_probability_cutoff(212);
        }
